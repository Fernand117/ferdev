{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "AngularRun",
            "request": "launch",
            "command": "ng serve -o",
            "cwd": "${workspaceFolder}/ferdev"
        }
    ]
}